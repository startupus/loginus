# Исправления проблем с бэкендом

## ✅ Исправлено:

### 1. Прокси в Vite
**Проблема:** Прокси был настроен на `http://loginus-api:3001` (имя Docker контейнера), но frontend запущен локально и не может разрешить это имя.

**Решение:** Изменен `target` в `vite.config.ts` на `http://localhost:3004` (порт, на который проброшен контейнер loginus-api).

**Файл:** `frontend/vite.config.ts`

### 2. Frontend перезапущен
Frontend перезапущен с новыми настройками прокси.

## 🔍 Проверка:

Прямые запросы к бэкенду работают:
- ✅ `http://localhost:3004/api/v2/translations/ru?modules=common` → 200
- ✅ `http://localhost:3004/api/v2/auth/flow` → 200
- ✅ `http://localhost:3000/api/v2/translations/ru?modules=common` (через прокси) → 200

## 📝 Что делать:

1. **Обновите страницу в браузере** (Ctrl+F5 или Cmd+Shift+R для полной перезагрузки)
2. **Очистите кэш браузера**, если ошибки продолжаются
3. **Проверьте консоль браузера** - там должны быть логи прокси: `[Vite Proxy] GET /api/v2/...`

## ⚠️ Если ошибки 500 продолжаются:

1. Проверьте логи бэкенда в реальном времени:
   ```bash
   docker-compose logs -f loginus-api
   ```

2. Откройте Network tab в DevTools браузера и проверьте:
   - URL запроса
   - Статус код
   - Response body (там будет детальная ошибка)

3. Проверьте, что бэкенд действительно запущен:
   ```bash
   docker-compose ps
   ```

