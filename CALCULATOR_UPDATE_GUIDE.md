# 🔧 ИСПРАВЛЕНО: API_BASE и размер виджета

## ✅ ЧТО ИСПРАВЛЕНО:

### 1️⃣ **Размер виджета адаптирован:**
- ✅ Контейнер виджета: `height: auto, minHeight: 200px`
- ✅ Iframe внутри: `height: 100%, minHeight: 250px`
- ✅ Виджет теперь растягивается под содержимое

### 2️⃣ **API_BASE в калькуляторе:**
**ПРОБЛЕМА:** Калькулятор отправлял запросы на `http://localhost:3004/api/v2`, но:
- Backend внутри Docker доступен на порту **3001**
- Frontend на порту **3000** проксирует `/api` на backend

**РЕШЕНИЕ:**
```javascript
const API_BASE = window.location.origin.includes('localhost:3000') 
    ? 'http://localhost:3000/api/v2'  // ✅ Через Vite proxy
    : 'http://localhost:3004/api/v2'; // Напрямую к backend
```

Теперь калькулятор **автоматически** определяет правильный URL!

---

## 🚀 КАК ОБНОВИТЬ:

### **ШАГ 1: Удалите старый плагин**
1. Откройте: http://localhost:3000/ru/admin/extensions
2. Найдите "Умный калькулятор" (или "calculator-advanced")
3. Нажмите "Удалить"

### **ШАГ 2: Удалите файлы плагина из Docker**
```powershell
docker exec loginus-api-new sh -c "rm -rf /app/uploads/plugins/calculator-advanced"
docker exec loginus-api-new sh -c "rm -rf /app/uploads/plugins/umnyy-kalkulyator"
```

### **ШАГ 3: Загрузите новую версию**
1. Откройте: http://localhost:3000/ru/admin/plugins/upload
2. Загрузите **НОВЫЙ** `calculator-advanced.zip`
3. Название: `Умный калькулятор v2`
4. Включить: ✅

### **ШАГ 4: Обновите пункт меню**
1. Откройте: http://localhost:3000/ru/admin/menu-settings
2. Найдите пункт "Калькулятор"
3. Нажмите "Изменить"
4. Выберите новый плагин "Умный калькулятор v2"
5. Сохраните

### **ШАГ 5: Откройте калькулятор и проверьте**
1. Перейдите в личный кабинет
2. Нажмите "Калькулятор" в меню
3. Выполните вычисление: `2 + 2 =`
4. **ВАЖНО:** Откройте DevTools (F12) → Console
5. Должны увидеть запросы к `/api/v2/events/emit`

### **ШАГ 6: Проверьте backend**
```powershell
# Логи (в отдельном окне PowerShell)
docker logs -f loginus-api-new

# В калькуляторе выполните вычисление 3 + 3
# Должны увидеть в логах:
# [EventBusService] Event dispatched: CALCULATOR_CALCULATION_DONE
```

### **ШАГ 7: Проверьте БД**
```powershell
docker exec loginus-db psql -U loginus -d loginus_dev -c "SELECT COUNT(*) FROM event_logs;"
```

Число должно быть > 0!

---

## 🐛 ПОЧЕМУ НЕ РАБОТАЛО:

### **Проблема 1: Неправильный порт API**
- Калькулятор: `http://localhost:3004/api/v2` ❌
- Реальный backend внутри Docker: `http://localhost:3001` ❌
- Правильный путь через Vite proxy: `http://localhost:3000/api/v2` ✅

### **Проблема 2: CORS**
Если запрос идёт напрямую к backend на порт 3001/3004, может быть CORS блокировка.

**Решение:** Использовать Vite proxy на порту 3000, который уже настроен в `vite.config.ts`:
```typescript
proxy: {
  '/api': {
    target: 'http://localhost:3004',
    changeOrigin: true,
  }
}
```

---

## ✅ ПОСЛЕ ОБНОВЛЕНИЯ:

- ✅ Калькулятор отправляет запросы на правильный URL
- ✅ События доходят до backend
- ✅ События логируются в БД
- ✅ Размер виджета адаптируется
- ✅ Виджет погоды отображается корректно

**Попробуйте обновить плагин и протестируйте снова!** 🚀

