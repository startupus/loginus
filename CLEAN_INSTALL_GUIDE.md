# 🎯 ЧИСТАЯ УСТАНОВКА - ПОШАГОВАЯ ИНСТРУКЦИЯ

## ✅ **ЧТО УЖЕ СДЕЛАНО:**
- ✅ Удалены все плагины из файловой системы (`/app/uploads/plugins/`)
- ✅ Удалены все временные файлы
- ✅ Система готова к чистой установке

---

## 🚀 **ТЕПЕРЬ ДЕЛАЕМ ТАК:**

### **ШАГ 1: Удалите плагины из БД через админку**

1. Откройте: `http://localhost:3000/ru/admin/extensions`
2. Если там есть какие-то расширения → Удалите **ВСЕ**
3. Если список пуст → Отлично, переходите к Шагу 2

---

### **ШАГ 2: Удалите пункты меню с плагинами**

1. Откройте: `http://localhost:3000/ru/admin/menu-settings`
2. Найдите все пункты связанные с плагинами:
   - "калькулятор"
   - "222"
   - "2"
   - любые другие кастомные пункты
3. Удалите их все
4. Нажмите "Сохранить изменения"

---

### **ШАГ 3: Перезагрузите страницу**
**Нажмите `Ctrl + F5`** - полная перезагрузка с очисткой кеша

---

### **ШАГ 4: Загрузите калькулятор**

1. Откройте: `http://localhost:3000/ru/admin/extensions/plugins/upload`

2. Заполните форму:
   - **Название:** `Калькулятор`
   - **Тип:** `plugin` (или `menu_item` - оба работают)
   - **Включено:** ✅ **ОБЯЗАТЕЛЬНО ПОСТАВЬТЕ ГАЛОЧКУ!**
   - **Файл:** Нажмите "Выбрать файл"
     - Выберите `calculator-plugin.zip` (2878 bytes)
     - **НЕ `test-simple-plugin.zip`!** (1666 bytes)

3. Нажмите **"Загрузить плагин"**

4. **Дождитесь сообщения об успехе**

---

### **ШАГ 5: СРАЗУ проверьте backend логи**

Откройте PowerShell и выполните:
```powershell
docker logs --tail 50 loginus-api-new
```

**Должно быть:**
```
[ExtensionsController] uploadExtension called
[ExtensionsController] file: {
  originalname: 'calculator-plugin.zip',
  mimetype: 'application/x-zip-compressed',
  size: 2878,
  hasBuffer: true,
  bufferLength: 2878
}
[ExtensionUploadService] Saved temporary .zip file: ...
[ExtensionUploadService] Extracting .zip to: /app/uploads/plugins/kalkulyator
[ExtensionUploadService] Extraction complete
[ExtensionUploadService] Read manifest: {"name":"calculator-plugin",...}
✅ Extension "Калькулятор" installed successfully
```

**Если НЕТ этих строк или есть ошибки** → Покажите мне логи!

---

### **ШАГ 6: Проверьте что файлы распаковались**

В PowerShell:
```powershell
docker exec loginus-api-new ls -la /app/uploads/plugins/
```

**Должна быть папка** (например `kalkulyator` или `calculator-plugin`)

Затем проверьте содержимое:
```powershell
docker exec loginus-api-new ls -la /app/uploads/plugins/kalkulyator/
```

**Должно быть:**
```
manifest.json
index.html
README.md
```

---

### **ШАГ 7: Добавьте калькулятор в меню**

1. Откройте: `http://localhost:3000/ru/admin/menu-settings`

2. Нажмите **"Добавить пункт меню"**

3. В модальном окне:
   - **Тип:** Выберите `iframe`
   - **Плагин:** В dropdown выберите **"Калькулятор"**

4. **ВАЖНО! ПРОВЕРЬТЕ** что поля автоматически заполнились:
   - **Icon:** `calculator` (или другой)
   - **Название (RU):** `Калькулятор`
   - **Название (EN):** `Calculator`
   - **iframeUrl:** Должен быть **примерно так:**
     ```
     http://localhost:3000/uploads/plugins/kalkulyator/index.html
     ```

5. **КРИТИЧЕСКИ ВАЖНО:**
   - URL **НЕ должен быть пустым!**
   - URL **ДОЛЖЕН содержать** `/uploads/plugins/`
   - URL **ДОЛЖЕН заканчиваться** на `/index.html`
   - URL **НЕ ДОЛЖЕН** быть `http://localhost:3000/ru/...`

6. Если URL правильный → Нажмите **"Сохранить"**

7. Если URL **пустой или неправильный**:
   - Скопируйте мне что там написано
   - **НЕ сохраняйте!**
   - Покажите мне скриншот

---

### **ШАГ 8: МОМЕНТ ИСТИНЫ! Проверьте калькулятор!**

1. Откройте: `http://localhost:3000/ru/profile`

2. В меню слева должен появиться **"🧮 Калькулятор"**

3. **КЛИКНИТЕ НА НЕГО!**

4. **ОЖИДАЕМЫЙ РЕЗУЛЬТАТ:**
   ```
   🎨 Фиолетовый градиентный фон (от #667eea до #764ba2)
   🧮 Заголовок "🧮 Калькулятор"
   🟢 Дисплей с зелёными цифрами на чёрном фоне
   🔢 Кнопки: 0-9, +, -, ×, /, %, C, =
   ✨ Анимации при нажатии
   ```

5. **НЕ ДОЛЖНО БЫТЬ:**
   - ❌ Главной страницы сайта
   - ❌ "Встроенный контент"
   - ❌ Белого экрана
   - ❌ Текста "Plugin Test"
   - ❌ Нашего логотипа Loginus

---

## 📸 **ПОКАЖИТЕ МНЕ:**

1. **Если калькулятор работает** → Скриншот открытого калькулятора
2. **Если НЕ работает:**
   - Скриншот того что видите
   - Backend логи: `docker logs --tail 50 loginus-api-new`
   - Скриншот поля `iframeUrl` из настроек меню

---

## 🌤️ **ПОСЛЕ КАЛЬКУЛЯТОРА - ВИДЖЕТ ПОГОДЫ (опционально):**

Если калькулятор работает, можно загрузить виджет:

1. `/ru/admin/extensions/widgets/upload`
2. Название: `Погода в Москве`
3. Тип: `widget`
4. Включено: ✅
5. Файл: `moscow-weather-widget.zip` (4019 bytes)
6. Загрузить

Виджет появится на дашборде автоматически с данными:
- 📍 Москва
- 🌡️ -5°C
- ☁️ Облачно
- 💧 Влажность, давление, ветер

---

**Начинайте с Шага 1 и показывайте результат на каждом шаге! 🚀**

