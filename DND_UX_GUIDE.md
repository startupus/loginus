# 💡 Как работает Drag-and-Drop в настройках меню

## 🎯 **Текущее поведение:**

### 1. **Перетащить НА элемент** → Создать вложенность
- Берёте элемент A
- Перетаскиваете НА элемент B (родителя)
- Элемент A становится дочерним для B
- Автоматически обновляется `path`: `/parent/child`

### 2. **Перетащить МЕЖДУ элементами** → Изменить порядок
- Работает только для элементов одного уровня (root)
- Меняет order элементов

### 3. **Перетащить вложенный элемент НА root** → Извлечь из вложенности
- Берёте вложенный элемент
- Перетаскиваете на root элемент
- Элемент извлекается на верхний уровень

## 🔧 **Улучшения которые нужны:**

### ✅ Уже реализовано:
- Логика вложенности
- Проверка `canBeNested` (нельзя вложить в себя, в своего ребёнка)
- Автоматическое обновление путей
- Рекурсивное удаление и добавление

### ⚠️ Нужно добавить для UX:

1. **Визуальный индикатор "drop zone"**
   - При hover над элементом показывать подсветку
   - Разные цвета для "вложить" vs "изменить порядок"

2. **Tooltip с подсказкой**
   - "Отпустите чтобы вложить в [Название]"
   - "Отпустите чтобы переместить после [Название]"

3. **Иконка действия**
   - ↳ для вложенности
   - ↕ для изменения порядка

4. **CSS классы для состояний**
   - `.dnd-can-nest` - можно вложить (зелёная подсветка)
   - `.dnd-can-reorder` - можно переставить (синяя подсветка)
   - `.dnd-cannot-drop` - нельзя (красная подсветка)

## 📝 **Текущие ограничения:**

- **Только 1 уровень вложенности** (строка 400: глубина = 1)
- **Нельзя вложить родителя в ребёнка**
- **Нельзя вложить элемент сам в себя**
- **Нельзя создать циклическую зависимость**

## 🎨 **Рекомендации по UX:**

Вместо сложных визуальных индикаторов, можно:

1. **Добавить кнопки действий прямо в UI:**
   ```
   [Название элемента]  [↓ Вложить в]  [↑ Извлечь]  [↕ Переместить]
   ```

2. **Модальное окно для вложенности:**
   - Кнопка "Создать вложенность"
   - Выбор родителя из dropdown

3. **Контекстное меню (ПКМ):**
   - Правый клик → "Вложить в..."
   - Правый клик → "Извлечь на верхний уровень"

Какой вариант предпочтительнее?

