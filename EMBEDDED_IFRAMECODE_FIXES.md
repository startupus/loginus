# ✅ Исправления: Embedded и iframeCode

## 🔧 Что исправлено

### 1. **Проблема с выбором плагина для Embedded**

**Проблема:** При выборе плагина автоматически перекидывало на страницу создания iframe, даже если выбран тип "внутреннее приложение".

**Исправление:**
- ✅ Сохранение текущего типа (iframe или embedded) при выборе плагина
- ✅ Если тип уже выбран (embedded), он не меняется на iframe
- ✅ Плагин заполняет соответствующие поля в зависимости от текущего типа
- ✅ Для embedded доступен выбор плагинов (как для iframe)

**Файлы:**
- `frontend/src/pages/admin/MenuSettingsPage.tsx` - функция `handlePluginSelect`

### 2. **Проблема с отображением iframeCode**

**Проблема:** При создании iframe с кодом (без плагина) содержимое кода не отображалось, а отображался сайт.

**Исправление:**
- ✅ Правильная декодировка кода из URL (`decodeURIComponent`)
- ✅ Улучшенная логика установки `about:blank` для iframe с кодом
- ✅ Правильная последовательность: установка `about:blank` → ожидание загрузки → запись кода
- ✅ Добавлено логирование для отладки
- ✅ Обработка ошибок при записи кода

**Файлы:**
- `frontend/src/pages/IframePage.tsx` - обработка `iframeCode`

### 3. **Улучшение формы для Embedded**

**Изменения:**
- ✅ Embedded использует ту же логику что и iframe
- ✅ Поля для embedded аналогичны iframe (но без поля для кода)
- ✅ Можно выбирать плагины для embedded
- ✅ Поле URL для embedded приложения

**Файлы:**
- `frontend/src/pages/admin/MenuSettingsPage.tsx` - форма для embedded

## 📋 Как теперь работает

### **Iframe с кодом:**
1. Создайте пункт меню типа "Iframe"
2. Вставьте HTML код в поле "HTML код"
3. Укажите путь (например: `/test-widget`)
4. Сохраните
5. **Код теперь правильно отображается в iframe**

### **Embedded с плагином:**
1. Создайте пункт меню типа "Внутреннее приложение"
2. Выберите плагин из списка
3. **Тип остается "embedded", не меняется на iframe**
4. Поля автоматически заполняются
5. Сохраните

### **Embedded без плагина:**
1. Создайте пункт меню типа "Внутреннее приложение"
2. Укажите URL вручную
3. Укажите путь
4. Сохраните

## ✅ Итог

- ✅ **Embedded** - сохраняет тип при выборе плагина
- ✅ **iframeCode** - правильно отображается в iframe
- ✅ **Единая логика** - embedded и iframe работают одинаково (кроме поддержки кода)

**Все проблемы исправлены!**

