# Реализация альтернативной системы i18n v2 для Loginus UI

## ✅ Выполненные задачи

### Этап 1: API v2 на бэкенде ✅

Создана новая версия API для модульной загрузки переводов:

**Файлы:**
- `backend-mock/src/translations-v2/translations-v2.service.ts` - сервис для работы с переводами v2
- `backend-mock/src/translations-v2/translations-v2.controller.ts` - контроллер с эндпоинтами
- `backend-mock/src/translations-v2/translations-v2.module.ts` - модуль NestJS

**Эндпоинты:**
- `GET /api/v2/translations/:locale/:module` - загрузка отдельного модуля
- `GET /api/v2/translations/:locale?modules=a,b,c` - загрузка нескольких модулей
- `GET /api/v2/translations/:locale/version` - проверка версии переводов
- `GET /api/v2/translations/status` - статус и доступные модули

**Особенности:**
- Версионирование переводов (из package.json)
- Checksum для проверки целостности данных
- Поддержка модульной структуры (10 модулей)
- Предзагрузка критичных модулей (common, profile)

### Этап 2: Клиентская система v2 ✅

Создана полная клиентская система для работы с переводами v2:

**Файлы:**
- `frontend/src/services/i18n/v2/types.ts` - TypeScript типы
- `frontend/src/services/i18n/v2/config.ts` - конфигурация системы
- `frontend/src/services/i18n/v2/cache.ts` - IndexedDB кэширование
- `frontend/src/services/i18n/v2/api-client.ts` - API клиент
- `frontend/src/services/i18n/v2/loader.ts` - система загрузки с fallback
- `frontend/src/services/i18n/v2/toggle.ts` - feature toggle
- `frontend/src/services/i18n/v2/index.ts` - экспорт всех компонентов

**Функциональность:**
- Многоуровневый fallback (кэш → API → статические файлы → русский → ключ)
- IndexedDB кэширование с версионированием
- Автоматическая инвалидация кэша при обновлении версии
- Защита от бесконечной рекурсии при fallback
- Неблокирующие асинхронные операции

### Этап 3: Feature Toggle ✅

Создана система переключения между режимами работы:

**Режимы:**
- `static` - только статические файлы (текущая система)
- `dynamic` - только API v2 (новая система)
- `hybrid` - гибридный режим (API → кэш → статические файлы)

**Файлы:**
- `frontend/src/services/i18n/v2/toggle.ts` - функции переключения режимов
- `frontend/src/store/i18n-mode.ts` - Zustand store для управления режимом

**Конфигурация:**
- Через environment variables (`VITE_I18N_MODE`)
- Переключение в development через localStorage
- Автоматическое определение режима при старте

### Этап 4: Версионирование и Checksum ✅

Реализована система версионирования переводов:

**На бэкенде:**
- Версия из `package.json`
- Timestamp последнего обновления
- MD5 checksum для проверки целостности

**На фронтенде:**
- Проверка версии при загрузке модулей
- Автоматическая инвалидация кэша при изменении версии
- Сравнение версий для определения необходимости обновления

### Этап 5: IndexedDB кэширование ✅

Реализована система кэширования в IndexedDB:

**Особенности:**
- Долгосрочное кэширование (TTL настраивается)
- Версионирование кэша
- Автоматическая очистка устаревших записей
- Индексы для быстрого поиска

**Структура базы данных:**
- Название: `loginus-i18n-cache`
- Версия: 1
- Хранилище: `translations`
- Индексы: `locale`, `module`, `timestamp`, `version`

## 📁 Структура файлов

### Бэкенд
```
backend-mock/src/translations-v2/
├── translations-v2.service.ts    # Сервис для работы с переводами v2
├── translations-v2.controller.ts # Контроллер с эндпоинтами
└── translations-v2.module.ts     # Модуль NestJS

backend-mock/data/translations/v2/
├── ru/
│   ├── common.json
│   ├── dashboard.json
│   └── ... (остальные модули)
└── en/
    ├── common.json
    └── ... (остальные модули)
```

### Фронтенд
```
frontend/src/services/i18n/v2/
├── types.ts           # TypeScript типы
├── config.ts          # Конфигурация системы
├── cache.ts           # IndexedDB кэширование
├── api-client.ts      # API клиент
├── loader.ts          # Система загрузки с fallback
├── toggle.ts          # Feature toggle
├── index.ts           # Экспорт всех компонентов
├── README.md          # Документация
└── INTEGRATION.md     # Инструкция по интеграции

frontend/src/store/
└── i18n-mode.ts       # Zustand store для режима работы
```

## 🔧 Конфигурация

### Environment Variables

```bash
# Режим работы i18n системы
VITE_I18N_MODE=hybrid  # static | dynamic | hybrid

# Базовый URL для API v2
VITE_I18N_API_BASE_URL=/api/v2

# Версия API
VITE_I18N_API_VERSION=v2

# TTL кэша в миллисекундах (по умолчанию 5 минут)
VITE_I18N_CACHE_TTL=300000

# Интервал проверки версии переводов (по умолчанию 5 минут)
VITE_I18N_VERSION_CHECK_INTERVAL=300000
```

## 🚀 Использование

### Базовое использование

```typescript
import { loadModule } from '@/services/i18n/v2/loader';

// Загрузить модуль
const commonData = await loadModule('ru', 'common');
```

### Проверка режима работы

```typescript
import { getI18nMode, isDynamicI18nMode } from '@/services/i18n/v2/toggle';

const mode = getI18nMode(); // 'static' | 'dynamic' | 'hybrid'
const isDynamic = isDynamicI18nMode(); // true/false
```

### Переключение режима (development)

```typescript
import { setI18nMode } from '@/services/i18n/v2/toggle';

setI18nMode('dynamic'); // Переключить на динамический режим
```

## 📊 Стратегия загрузки

Система использует многоуровневый fallback:

1. **Level 1**: Кэш IndexedDB (если включен)
2. **Level 2**: API бэкенда v2 (если включен динамический режим)
3. **Level 3**: Статические JSON файлы (текущая система)
4. **Level 4**: Fallback на русский язык
5. **Level 5**: Показ ключа перевода вместо текста

## 🧪 Тестирование

### Проверка работы API

```bash
# Проверить статус
curl http://localhost:3001/api/v2/translations/status

# Загрузить модуль
curl http://localhost:3001/api/v2/translations/ru/common

# Загрузить несколько модулей
curl http://localhost:3001/api/v2/translations/ru?modules=common,dashboard

# Проверить версию
curl http://localhost:3001/api/v2/translations/ru/version
```

### Проверка клиентской части

```typescript
import { translationsAPI } from '@/services/i18n/v2/api-client';

// Проверить доступность API
const isAvailable = await translationsAPI.isAvailable();

// Загрузить модуль
const module = await translationsAPI.getModule('ru', 'common');
```

## 📝 Следующие шаги

### Этап 4: Интеграция и тестирование (в процессе)

1. ✅ Создать интеграционные тесты
2. ⏳ Интегрировать с существующей системой i18n
3. ⏳ Добавить unit-тесты для всех компонентов
4. ⏳ Проверить работу в разных браузерах
5. ⏳ Оптимизировать производительность

### Рекомендации

1. **Постепенная миграция**: Начать с гибридного режима для тестирования
2. **Мониторинг**: Отслеживать метрики загрузки и использования кэша
3. **A/B тестирование**: Протестировать на небольшой группе пользователей
4. **Документация**: Обновить документацию для команды

## 🔍 Отладка

В development режиме система логирует все операции:

- `[i18n-v2] Loaded {locale}/{module} from cache` - загрузка из кэша
- `[i18n-v2] Loaded {locale}/{module} from API` - загрузка через API
- `[i18n-v2] Loaded {locale}/{module} from static files` - загрузка из статических файлов
- `[i18n-v2] Falling back to ru for module {module}` - fallback на русский

## ⚠️ Важные замечания

1. **Обратная совместимость**: Система полностью обратно совместима с текущей системой
2. **Производительность**: В гибридном режиме сначала проверяется кэш, затем API, затем статические файлы
3. **Версионирование**: Кэш автоматически инвалидируется при изменении версии переводов
4. **Офлайн режим**: При отсутствии сети система автоматически использует кэш и статические файлы
5. **Безопасность**: Все операции неблокирующие и асинхронные, ошибки обрабатываются gracefully

## 📚 Документация

- [README.md](frontend/src/services/i18n/v2/README.md) - Полная документация системы
- [INTEGRATION.md](frontend/src/services/i18n/v2/INTEGRATION.md) - Инструкция по интеграции
- [I18N_ARCHITECTURE_ANALYSIS.md](I18N_ARCHITECTURE_ANALYSIS.md) - Архитектурный анализ текущей системы

