# Руководство по использованию системы переводов v2

## 🚀 Быстрый старт

### 1. Активация системы

Система v2 готова к использованию. Для активации установите переменную окружения:

```bash
# .env.development или .env.local
VITE_I18N_MODE=hybrid  # static | dynamic | hybrid
```

**Режимы работы:**
- `static` - только статические файлы (текущая система, по умолчанию)
- `dynamic` - только API v2 (новая система)
- `hybrid` - гибридный режим (API → кэш → статические файлы)

### 2. Запуск бэкенда

```bash
cd backend-mock
npm run start:dev
```

Бэкенд будет доступен на `http://localhost:3001`

### 3. Проверка работы API

```bash
# Использование скрипта
./scripts/test-i18n-api.sh

# Или вручную
curl http://localhost:3001/api/v2/translations/status
```

## 📝 Использование в коде

### Базовое использование

Система автоматически интегрируется с существующей системой i18n. Просто используйте `useTranslation` как обычно:

```typescript
import { useTranslation } from 'react-i18next';

function MyComponent() {
  const { t } = useTranslation();
  
  return <div>{t('common.today')}</div>;
}
```

### Переключение режима в development

```typescript
import { setI18nMode } from '@/services/i18n/v2/toggle';

// Переключить на динамический режим
setI18nMode('dynamic');

// Переключить на гибридный режим
setI18nMode('hybrid');

// Вернуться к статическому режиму
setI18nMode('static');
```

### Прямое использование v2 системы

```typescript
import { loadModule } from '@/services/i18n/v2/loader';

// Загрузить модуль
const commonData = await loadModule('ru', 'common');
console.log(commonData);
```

### Работа с кэшем

```typescript
import { translationCache } from '@/services/i18n/v2/cache';

// Получить из кэша
const cached = await translationCache.get('ru', 'common');

// Очистить кэш
await translationCache.clear();
```

### Проверка доступности API

```typescript
import { translationsAPI } from '@/services/i18n/v2/api-client';

const isAvailable = await translationsAPI.isAvailable();
if (isAvailable) {
  // API доступен, можно использовать динамический режим
}
```

## 🔧 Конфигурация

### Environment Variables

```bash
# Режим работы i18n системы
VITE_I18N_MODE=hybrid

# Базовый URL для API v2 (по умолчанию /api/v2)
VITE_I18N_API_BASE_URL=/api/v2

# Версия API (по умолчанию v2)
VITE_I18N_API_VERSION=v2

# TTL кэша в миллисекундах (по умолчанию 5 минут)
VITE_I18N_CACHE_TTL=300000

# Интервал проверки версии переводов (по умолчанию 5 минут)
VITE_I18N_VERSION_CHECK_INTERVAL=300000
```

## 🧪 Тестирование

### Запуск unit-тестов

```bash
# Фронтенд
cd frontend
npm test

# Бэкенд
cd backend-mock
npm test
```

### Ручное тестирование API

См. [API_TEST.md](backend-mock/src/translations-v2/API_TEST.md)

### Использование тестового скрипта

```bash
./scripts/test-i18n-api.sh
```

## 📊 Мониторинг

В development режиме система логирует все операции:

- `[i18n-v2] Loaded {locale}/{module} from cache` - загрузка из кэша
- `[i18n-v2] Loaded {locale}/{module} from API` - загрузка через API
- `[i18n-v2] Loaded {locale}/{module} from static files` - загрузка из статических файлов
- `[i18n-v2] Falling back to ru for module {module}` - fallback на русский

## 🔄 Миграция с текущей системы

### Вариант 1: Постепенная миграция (рекомендуется)

1. Установите `VITE_I18N_MODE=hybrid`
2. Система автоматически будет использовать API при доступности, иначе статические файлы
3. Мониторьте логи и метрики
4. После проверки переключите на `dynamic`

### Вариант 2: Использование интегрированного config

Замените импорт в `main.tsx`:

```typescript
// Было
import './services/i18n';

// Стало
import './services/i18n/config-integrated';
```

### Вариант 3: Полная замена

См. [INTEGRATION.md](frontend/src/services/i18n/v2/INTEGRATION.md)

## 🐛 Отладка

### Проблема: Переводы не загружаются

**Решение:**
1. Проверьте режим работы: `getI18nMode()`
2. Проверьте доступность API: `translationsAPI.isAvailable()`
3. Проверьте логи в консоли браузера
4. Проверьте кэш: `translationCache.get('ru', 'common')`

### Проблема: API недоступен

**Решение:**
1. Убедитесь, что бэкенд запущен
2. Проверьте URL: `VITE_I18N_API_BASE_URL`
3. Проверьте CORS настройки на бэкенде
4. Используйте гибридный режим для fallback на статические файлы

### Проблема: Кэш не работает

**Решение:**
1. Проверьте поддержку IndexedDB в браузере
2. Проверьте логи в консоли
3. Очистите кэш: `translationCache.clear()`

## 🎨 Форматирование чисел и дат

Для поддержки мультиязычности используйте утилиты из `utils/intl/formatters.ts`:

```typescript
import { formatNumber, formatCurrency, formatDate, formatRelativeTimeWithT } from '@/utils/intl/formatters';
import { useCurrentLanguage } from '@/utils/routing';
import { useTranslation } from 'react-i18next';

function MyComponent() {
  const { t } = useTranslation();
  const currentLang = useCurrentLanguage();
  
  // Форматирование чисел
  const formattedNumber = formatNumber(1234567, currentLang); // "1,234,567" или "1 234 567"
  
  // Форматирование валюты
  const formattedCurrency = formatCurrency(1000, 'RUB', currentLang); // "1 000 ₽" или "RUB 1,000"
  
  // Форматирование даты
  const formattedDate = formatDate(new Date(), currentLang); // "23 янв" или "Jan 23"
  
  // Относительное время (требует функцию перевода)
  const relativeTime = formatRelativeTimeWithT(new Date(), t, currentLang); // "Сегодня" или "Today"
}
```

**Важно:** Не используйте хардкодные локали типа `'ru-RU'` или `'en-US'`. Всегда используйте форматтеры с текущим языком.

## 📋 Эталонный метод для страниц

См. [Dashboard/README.md](frontend/src/components/Dashboard/README.md) для чеклиста и эталонного метода тиражирования на другие страницы.

## 📚 Дополнительная документация

- [README.md](frontend/src/services/i18n/v2/README.md) - Полная документация системы
- [INTEGRATION.md](frontend/src/services/i18n/v2/INTEGRATION.md) - Инструкция по интеграции
- [API_TEST.md](backend-mock/src/translations-v2/API_TEST.md) - Тестирование API
- [I18N_V2_IMPLEMENTATION.md](I18N_V2_IMPLEMENTATION.md) - Описание реализации
- [Dashboard/README.md](frontend/src/components/Dashboard/README.md) - Эталонный метод для страниц

## ✅ Чеклист для начала работы

- [ ] Бэкенд запущен и доступен
- [ ] API эндпоинты работают (проверено через `test-i18n-api.sh`)
- [ ] Установлена переменная `VITE_I18N_MODE`
- [ ] Проверена работа в браузере (открыть DevTools → Network)
- [ ] Проверены логи в консоли
- [ ] Протестирована загрузка переводов
- [ ] Проверен fallback на статические файлы

## 🎯 Следующие шаги

1. **Тестирование**: Протестируйте систему в development режиме
2. **Мониторинг**: Отслеживайте метрики загрузки и использования кэша
3. **Оптимизация**: Настройте TTL кэша и интервалы проверки версий
4. **Документация**: Обновите документацию для команды
5. **A/B тестирование**: Протестируйте на небольшой группе пользователей

## 💡 Советы

1. **Начните с гибридного режима** - это обеспечит плавный переход
2. **Мониторьте производительность** - проверяйте время загрузки модулей
3. **Используйте кэш** - он значительно ускоряет загрузку
4. **Тестируйте fallback** - убедитесь, что система работает при ошибках API
5. **Документируйте изменения** - фиксируйте все изменения в переводах

