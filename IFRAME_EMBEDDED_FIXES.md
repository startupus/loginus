# ✅ Исправления: iframeCode и упрощение Embedded

## 🔧 Что исправлено

### 1. **Проблема с iframeCode**

**Проблема:** При создании iframe с кодом (не плагином) открывалась копия сайта вместо содержимого.

**Причина:** 
- Iframe не использовал `about:blank` для записи кода
- Токен не отправлялся для iframe с кодом

**Исправление:**
- ✅ Используется `about:blank` для iframe с кодом
- ✅ Код записывается после загрузки `about:blank`
- ✅ Токен отправляется через postMessage после записи кода
- ✅ Правильная обработка событий загрузки

**Файлы:**
- `frontend/src/pages/IframePage.tsx`

### 2. **Упрощение Embedded**

**Изменение:** Embedded теперь использует ту же логику что и iframe.

**Что сделано:**
- ✅ Embedded использует `/iframe` маршрут (как iframe)
- ✅ Embedded использует `embeddedAppUrl` как параметр `url`
- ✅ Можно выбирать плагины для embedded (как для iframe)
- ✅ Та же обработка в `IframePage`

**Преимущества:**
- Проще для разработчиков
- Единая логика обработки
- Меньше кода для поддержки
- Работает сразу без дополнительной настройки

**Файлы:**
- `frontend/src/design-system/layouts/PageTemplate/PageTemplate.tsx`
- `frontend/src/pages/admin/MenuSettingsPage.tsx`

## 📋 Текущее состояние типов

### **External (Внешняя ссылка)**
- ✅ Простое перенаправление на URL
- ✅ Поле для пути в меню
- ✅ Чекбокс "Открыть в новой вкладке"
- ✅ Не использует плагины

### **Iframe (Встроенный контент)**
- ✅ Поддержка `iframeUrl` (из архива плагина)
- ✅ Поддержка `iframeCode` (HTML код) - **ИСПРАВЛЕНО**
- ✅ Выбор плагинов для автозаполнения
- ✅ Передача токена через postMessage
- ✅ Динамическое изменение размера

### **Embedded (Внутреннее приложение)**
- ✅ Использует ту же логику что и iframe - **УПРОЩЕНО**
- ✅ Поддержка `embeddedAppUrl` (как `iframeUrl`)
- ✅ Выбор плагинов для автозаполнения
- ✅ Передача токена через postMessage
- ✅ Рендерится через `IframePage`

## 🎯 Как использовать

### **Iframe с кодом:**
1. Создайте пункт меню типа "Iframe"
2. Вставьте HTML код в поле "HTML код"
3. Укажите путь (например: `/test-widget`)
4. Сохраните

**Пример кода:** Используйте `test-iframe-code.html`

### **Embedded приложение:**
1. Создайте пункт меню типа "Внутреннее приложение"
2. Выберите плагин (если есть) или укажите URL вручную
3. Укажите путь (например: `/embedded/my-app`)
4. Сохраните

**Примечание:** Embedded работает так же как iframe, просто другой тип для семантики.

## ✅ Итог

- ✅ **iframeCode** - исправлено, теперь работает корректно
- ✅ **Embedded** - упрощено, использует ту же логику что и iframe
- ✅ **Единая логика** - проще поддерживать и развивать

**Все типы теперь работают корректно!**

