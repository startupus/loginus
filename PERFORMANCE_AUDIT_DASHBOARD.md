# Аудит производительности Dashboard

## 🔴 КРИТИЧЕСКИЕ ПРОБЛЕМЫ

### 1. DashboardPage загружается синхронно
**Файл**: `frontend/src/router/routes.tsx:2`
**Проблема**: 
```typescript
import DashboardPage from '@/pages/DashboardPage'; // ❌ СИНХРОННЫЙ!
```
Вместо lazy() используется обычный импорт, что загружает всю страницу при старте приложения.

**Решение**:
```typescript
const DashboardPage = lazy(() => import('@/pages/DashboardPage'));
```

---

### 2. Множество синхронных импортов в DashboardPage
**Файл**: `frontend/src/pages/DashboardPage.tsx:4-10`
**Проблема**: Компоненты, которые не всегда нужны, загружаются синхронно:
- `PageTemplate` - загружает Header, Sidebar, Footer
- `WidgetSelector` - модалка выбора виджетов
- `MasonryGrid` - layout компонент
- `AddWidgetCard` - карточка добавления виджета

**Решение**: Сделать lazy для необязательных компонентов:
```typescript
const WidgetSelector = lazy(() => import('../components/Dashboard/WidgetSelector'));
const AddWidgetCard = lazy(() => import('../components/Dashboard/AddWidgetCard'));
```

---

### 3. i18n блокирует загрузку
**Файл**: `frontend/src/main.tsx:8`
**Проблема**:
```typescript
import './services/i18n'; // Загружает ВСЕ переводы синхронно
```

**Решение**: Уже реализован lazy loading в `i18n/config.ts`, но импорт все равно блокирует старт.

---

### 4. React.StrictMode в development
**Файл**: `frontend/src/main.tsx:35`
**Проблема**: В dev режиме рендерит все дважды для поиска багов.

**Решение**: Это нормально для dev, но в production его не будет.

---

### 5. Множество Context провайдеров
**Файл**: `frontend/src/main.tsx:36-44`
**Проблема**: 5 уровней вложенности:
- ThemeProvider
- ClientProvider  
- LanguageProvider
- QueryClientProvider
- ErrorBoundary

**Решение**: Объединить провайдеры в один RootProvider для оптимизации.

---

## 📊 МЕТРИКИ (до оптимизации)

### Время загрузки (локально):
- **Initial Load**: ~500-800ms
- **DashboardPage**: ~300-500ms  
- **API Request**: ~50-100ms (мок данные)

### Размер бандла:
- **Main bundle**: ~800KB (неоптимизированный)
- **Vendor chunks**: react-vendor (~200KB), utils (~150KB)
- **i18n JSON**: ru.json + en.json (~50KB каждый)

---

## ✅ ПЛАН ОПТИМИЗАЦИИ

### Приоритет 1: Критические исправления
1. ✅ Сделать DashboardPage lazy
2. ✅ Lazy load WidgetSelector и AddWidgetCard
3. ✅ Оптимизировать дизайн-систему (уже сделано)
4. ✅ Lazy load i18n (уже сделано)

### Приоритет 2: Улучшения
5. Объединить провайдеры в RootProvider
6. Добавить preloading для критических компонентов
7. Оптимизировать импорты из design-system

### Приоритет 3: Мониторинг
8. Добавить React DevTools Profiler
9. Lighthouse audit
10. Bundle analyzer

---

## 🎯 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

После оптимизации:
- **Initial Load**: 200-300ms (-60%)
- **DashboardPage**: 100-200ms (-65%)
- **Main bundle**: 400-500KB (-50%)
- **Time to Interactive**: <1s (было ~2s)

---

## 🔧 НЕМЕДЛЕННЫЕ ДЕЙСТВИЯ

1. Исправить routes.tsx - сделать DashboardPage lazy
2. Сделать lazy компоненты в DashboardPage
3. Убрать StrictMode для production (уже настроено Vite)
4. Проверить network waterfall в DevTools

---

## 📝 ДОПОЛНИТЕЛЬНЫЕ ПРОБЛЕМЫ

### localStorage операции
**Файл**: `DashboardPage.tsx:61-83`
Два синхронных чтения из localStorage при каждом рендере:
- `widgetOrder`
- `enabledWidgets`

**Решение**: Вынести в отдельный hook с мемоизацией.

### React Query настройки
**Файл**: `DashboardPage.tsx:85-96`
```typescript
staleTime: Infinity, // ❌ Данные никогда не обновляются
refetchOnMount: false, // ❌ Не обновляется при переходе
```

Это может привести к устаревшим данным, но зато быстро.

---

## 🚀 КОМАНДЫ ДЛЯ АНАЛИЗА

```bash
# Анализ размера бандла
cd frontend
npm run build
npx vite-bundle-visualizer

# Lighthouse audit
npx lighthouse http://localhost:3000/ru/dashboard --view

# React DevTools Profiler
# Открыть DevTools → Profiler → Start profiling
```

