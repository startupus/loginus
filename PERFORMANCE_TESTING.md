# Инструкция по тестированию производительности

## 🔍 Как проверить скорость загрузки

### 1. Откройте DevTools в браузере

**Chrome/Edge:**
- `F12` или `Cmd+Option+I` (Mac) / `Ctrl+Shift+I` (Windows)
- Перейдите на вкладку **Console**

### 2. Откройте страницу Dashboard

Перейдите на: `http://localhost:3000/ru/dashboard`

### 3. Смотрите логи в консоли

Вы увидите метрики производительности:

```
[Dashboard] i18n modules loaded: 15.23ms
[API] GET /api/v1/profile/dashboard: 12.45ms
[Dashboard] API request: 12.45ms
[Dashboard] Component render: 8.67ms
```

### 4. Проверьте Network tab

**Chrome DevTools → Network:**
- Найдите запрос `/api/v1/profile/dashboard`
- Проверьте **Time** колонку
- Должно быть <50ms для локального бэкенда

### 5. Проверьте Performance tab

**Chrome DevTools → Performance:**
1. Нажмите **Record** (кружок)
2. Перезагрузите страницу (`Cmd+R` / `Ctrl+R`)
3. Подождите полной загрузки
4. Нажмите **Stop**
5. Посмотрите на **Timing** секцию:
   - **DOMContentLoaded** - должно быть <200ms
   - **Load** - должно быть <500ms
   - **First Contentful Paint** - должно быть <300ms

---

## 📊 Что означают метрики

### [Dashboard] i18n modules loaded
- Время загрузки модулей переводов
- Нормально: <50ms
- Проблема: >100ms

### [API] GET /api/v1/profile/dashboard
- Время ответа от бэкенда
- Нормально: <50ms (локально)
- Проблема: >200ms

### [Dashboard] API request
- Время выполнения запроса с учетом всех задержек
- Нормально: <100ms
- Проблема: >300ms

### [Dashboard] Component render
- Время рендеринга компонента
- Нормально: <50ms
- Проблема: >200ms

---

## 🔴 Типичные проблемы

### Проблема 1: Долгая загрузка i18n модулей (>100ms)

**Причина:**
- Модули загружаются синхронно
- Большие JSON файлы

**Решение:**
- Уже реализовано: модули загружаются асинхронно
- Проверьте размер файлов в `Network` tab

### Проблема 2: Долгий API запрос (>200ms)

**Причина:**
- Бэкенд медленно отвечает
- Проблемы с сетью
- Блокирующие операции

**Решение:**
- Проверьте логи бэкенда
- Проверьте кэширование в `ProfileService`
- Проверьте размер ответа

### Проблема 3: Долгий рендеринг (>200ms)

**Причина:**
- Много компонентов рендерятся одновременно
- Тяжелые вычисления
- Большое количество DOM элементов

**Решение:**
- Используйте React DevTools Profiler
- Найдите компоненты с долгим рендерингом
- Оптимизируйте через `useMemo` / `useCallback`

---

## 🎯 Ожидаемые значения

### Первая загрузка (холодный старт):
- i18n modules: 20-50ms
- API request: 10-30ms
- Component render: 30-80ms
- **Total: 60-160ms**

### Повторная загрузка (теплый кэш):
- i18n modules: 0-5ms (из кэша)
- API request: 0-10ms (из React Query кэша)
- Component render: 20-50ms
- **Total: 20-65ms**

---

## 🐛 Если загрузка нестабильная

### Проверьте:

1. **Кэш браузера:**
   - Откройте DevTools → Network
   - Включите "Disable cache"
   - Перезагрузите страницу

2. **React Query кэш:**
   - В консоли: `localStorage.clear()`
   - Перезагрузите страницу

3. **Множественные процессы:**
   ```bash
   # Проверьте процессы Vite
   lsof -ti:3000
   lsof -ti:3001
   
   # Убейте лишние процессы
   killall node
   ```

4. **Размер бандла:**
   - DevTools → Network → JS файлы
   - Проверьте размер каждого чанка
   - Должно быть <500KB для initial load

---

## 📝 Что делать если медленно

1. **Откройте консоль** и посмотрите логи
2. **Найдите самое долгое** (самое большое число в ms)
3. **Проверьте Network tab** - какой запрос долгий
4. **Проверьте Performance tab** - какой компонент долго рендерится
5. **Сообщите результаты** - какие метрики показывают проблему

---

## 🔧 Дополнительная диагностика

### React DevTools Profiler

1. Установите React DevTools расширение
2. Откройте Profiler tab
3. Нажмите Record
4. Перезагрузите страницу
5. Остановите запись
6. Найдите компоненты с долгим рендерингом (красные)

### Lighthouse Audit

1. DevTools → Lighthouse
2. Выберите "Performance"
3. Нажмите "Generate report"
4. Посмотрите метрики:
   - First Contentful Paint
   - Time to Interactive
   - Total Blocking Time

---

**Важно:** Все логи работают только в `development` режиме. В production они автоматически удаляются.

