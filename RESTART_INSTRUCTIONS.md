# Инструкции по перезапуску сервисов после исправлений

## ✅ Исправленные проблемы:

1. **Кнопка "Выбрать файл"** - добавлено логирование и исправлен обработчик
2. **Удаление пунктов меню** - исправлена логика на бэкенде, плагины больше не восстанавливаются
3. **DnD вложенность** - добавлена возможность вытаскивать элементы из вложенности, улучшена логика

## 🔄 Перезапуск сервисов:

### Frontend (локально):
```bash
# Остановите текущий процесс (Ctrl+C в терминале где запущен frontend)
# Затем перезапустите:
cd frontend
npm run dev
# или
npm start
```

### Backend (Docker контейнер):
```bash
# Перезапустите контейнер с бэкендом
docker-compose restart backend
# или если используете отдельный контейнер:
docker restart <container-name>

# Если нужно пересобрать:
docker-compose up -d --build backend
```

### Полный перезапуск (если используете docker-compose):
```bash
docker-compose restart
```

## 🔍 Проверка исправлений:

### 1. Кнопка выбора файла:
- Откройте консоль браузера (F12)
- Перейдите на страницу загрузки плагина
- Нажмите "Выбрать файл"
- В консоли должны появиться логи: `[PluginsUploadPage] Button clicked...`

### 2. Удаление пунктов меню:
- Удалите пункт меню в админ-панели
- Перезагрузите страницу (F5)
- Удаленный пункт НЕ должен восстановиться

### 3. DnD вложенность:
- Перетащите один пункт на другой - должен создаться вложенный элемент
- Перетащите вложенный элемент на корневой уровень - должен вытащиться обратно
- Проверьте в консоли логи: `[MenuSettingsPage] handleDragEnd:...`

## 📝 Логи для отладки:

Все исправления включают логирование в консоль браузера (для frontend) и в логи контейнера (для backend).

Для просмотра логов бэкенда:
```bash
docker-compose logs -f backend
```

