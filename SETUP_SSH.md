# 🔐 Настройка SSH ключа для CI/CD

## Шаг 1: Получение приватного SSH ключа

Откройте PowerShell и выполните:

```powershell
Get-Content C:\Users\teramisuslik\.ssh\id_ed25519
```

**ВАЖНО:** Скопируйте ВЕСЬ вывод полностью, включая:
- Первую строку: `-----BEGIN OPENSSH PRIVATE KEY-----`
- Все строки посередине (это зашифрованный ключ)
- Последнюю строку: `-----END OPENSSH PRIVATE KEY-----`

Пример того, что должно быть скопировано:
```
-----BEGIN OPENSSH PRIVATE KEY-----
b3BlbnNzaC1rZXktdjEAAAAABG5vbmUAAAAEbm9uZQAAAAAAAAABAAAAMwAAAAtzc2gtZW
QyNTUxOQAAACD... (много строк) ...
... (много строк) ...
-----END OPENSSH PRIVATE KEY-----
```

## Шаг 2: Добавление секрета в GitHub

1. На странице, которую вы видите, нажмите зеленую кнопку **"Новый секрет хранилища"** (New repository secret)

2. В открывшейся форме:
   - **Имя (Name):** введите `SSH_PRIVATE_KEY` (точно так, без пробелов)
   - **Секрет (Secret):** вставьте ВЕСЬ приватный ключ, который скопировали в шаге 1
     - Вставьте всё: от `-----BEGIN` до `-----END`
     - Включая все строки между ними

3. Нажмите **"Добавить секрет"** (Add secret)

**Проверка:** После добавления вы должны увидеть секрет `SSH_PRIVATE_KEY` в списке "Секреты хранилища"

## Шаг 3: Проверка доступа

Убедитесь, что публичный ключ добавлен на сервер:

```bash
ssh -i C:\Users\teramisuslik\.ssh\id_ed25519 root@45.144.176.42 "echo 'SSH connection successful'"
```

Если подключение не работает, добавьте публичный ключ:

```bash
type C:\Users\teramisuslik\.ssh\id_ed25519.pub | ssh root@45.144.176.42 "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys"
```

## Готово! 

Теперь при каждом push в `main` будет автоматически запускаться деплой на сервер.

