# Инструкция по тестированию системы i18n v2

## ⚠️ Важно: Перезапуск сервера

После внесения изменений в код бэкенда необходимо **перезапустить сервер**:

```bash
# Остановите текущий процесс (Ctrl+C или kill)
# Затем запустите заново:
cd backend-mock
npm run start:dev
```

## 🧪 Тестирование API

После перезапуска сервера проверьте работу API:

```bash
# Проверка статуса
curl http://localhost:3001/api/v2/translations/status

# Загрузка модуля
curl http://localhost:3001/api/v2/translations/ru/common

# Загрузка нескольких модулей
curl "http://localhost:3001/api/v2/translations/ru?modules=common,dashboard"

# Проверка версии
curl http://localhost:3001/api/v2/translations/ru/version
```

## 🌐 Тестирование на странице

1. **Откройте браузер**: http://localhost:3000

2. **Откройте DevTools** (F12) → вкладка Console

3. **Проверьте логи** - должны быть сообщения:
   - `[i18n-v2] Loaded ru/common from API` (если включен динамический режим)
   - `[i18n-v2] Loaded ru/common from cache` (если используется кэш)
   - `[i18n-v2] Loaded ru/common from static files` (если используется fallback)

4. **Проверьте Network** - должны быть запросы к `/api/v2/translations/...`

5. **Проверьте IndexedDB**:
   - DevTools → Application → IndexedDB → `loginus-i18n-cache`
   - Должны быть записи для загруженных модулей

## 🔧 Активация v2 системы

Для активации v2 системы создайте файл `.env.local` в папке `frontend`:

```bash
# frontend/.env.local
VITE_I18N_MODE=hybrid
```

Или установите переменную окружения перед запуском:

```bash
VITE_I18N_MODE=hybrid npm run dev
```

## 📊 Проверка работы

### В консоли браузера выполните:

```javascript
// Проверка режима работы
import { getI18nMode } from './services/i18n/v2/toggle';
console.log('I18N Mode:', getI18nMode());

// Проверка доступности API
import { translationsAPI } from './services/i18n/v2/api-client';
const isAvailable = await translationsAPI.isAvailable();
console.log('API Available:', isAvailable);

// Загрузка модуля
import { loadModule } from './services/i18n/v2/loader';
const data = await loadModule('ru', 'common');
console.log('Loaded module:', data);

// Проверка кэша
import { translationCache } from './services/i18n/v2/cache';
const cached = await translationCache.get('ru', 'common');
console.log('Cached:', cached);
```

## ✅ Ожидаемые результаты

1. **API работает**: Запросы к `/api/v2/translations/...` возвращают данные
2. **Переводы загружаются**: Страница отображает переводы корректно
3. **Кэш работает**: Модули сохраняются в IndexedDB
4. **Fallback работает**: При ошибке API используются статические файлы
5. **Логи показывают источник**: В консоли видно, откуда загружаются переводы

## 🐛 Отладка

Если что-то не работает:

1. **Проверьте, что сервер перезапущен**
2. **Проверьте логи сервера** - должны быть сообщения о предзагрузке
3. **Проверьте CORS** - запросы должны проходить
4. **Проверьте Network** - запросы должны быть успешными
5. **Проверьте консоль браузера** - не должно быть ошибок

