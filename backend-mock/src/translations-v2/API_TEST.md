# Тестирование API переводов v2

## 🧪 Ручное тестирование эндпоинтов

### Предварительные требования

1. Запустить бэкенд сервер:
```bash
cd backend-mock
npm run start:dev
```

2. Убедиться, что сервер запущен на `http://localhost:3001`

### Тестирование эндпоинтов

#### 1. Проверка статуса системы

```bash
curl http://localhost:3001/api/v2/translations/status
```

**Ожидаемый ответ:**
```json
{
  "version": "1.0.0",
  "modules": ["common", "dashboard", "auth", "profile", "errors", "landing", "about", "work", "modals", "support"],
  "locales": ["ru", "en"],
  "lastUpdated": 1234567890
}
```

#### 2. Загрузка отдельного модуля

```bash
curl http://localhost:3001/api/v2/translations/ru/common
```

**Ожидаемый ответ:**
```json
{
  "version": "1.0.0",
  "locale": "ru",
  "module": "common",
  "data": {
    "common": {
      "today": "Сегодня",
      "yesterday": "Вчера",
      ...
    }
  },
  "timestamp": 1234567890,
  "checksum": "abc123..."
}
```

#### 3. Загрузка нескольких модулей

```bash
curl "http://localhost:3001/api/v2/translations/ru?modules=common,dashboard"
```

**Ожидаемый ответ:**
```json
{
  "success": true,
  "data": [
    {
      "version": "1.0.0",
      "locale": "ru",
      "module": "common",
      "data": {...},
      "timestamp": 1234567890,
      "checksum": "abc123..."
    },
    {
      "version": "1.0.0",
      "locale": "ru",
      "module": "dashboard",
      "data": {...},
      "timestamp": 1234567890,
      "checksum": "def456..."
    }
  ]
}
```

#### 4. Загрузка всех модулей для локали

```bash
curl http://localhost:3001/api/v2/translations/ru
```

**Ожидаемый ответ:**
```json
{
  "success": true,
  "data": {
    "common": {...},
    "dashboard": {...},
    "auth": {...},
    ...
  }
}
```

#### 5. Проверка версии переводов

```bash
curl http://localhost:3001/api/v2/translations/ru/version
```

**Ожидаемый ответ:**
```json
{
  "version": "1.0.0",
  "locale": "ru",
  "timestamp": 1234567890
}
```

#### 6. Тестирование английской локали

```bash
curl http://localhost:3001/api/v2/translations/en/common
```

#### 7. Тестирование несуществующего модуля

```bash
curl http://localhost:3001/api/v2/translations/ru/nonexistent
```

**Ожидаемый ответ:** Ошибка 500 с сообщением "Module nonexistent not found"

## 🔍 Проверка работы через браузер

1. Откройте браузер и перейдите на:
   - `http://localhost:3001/api/v2/translations/status`
   - `http://localhost:3001/api/v2/translations/ru/common`

2. Проверьте, что ответы корректны и содержат ожидаемые данные

## 🧪 Автоматическое тестирование

Запуск unit-тестов:

```bash
cd backend-mock
npm test
```

## 📊 Проверка производительности

### Проверка времени ответа

```bash
time curl http://localhost:3001/api/v2/translations/ru/common
```

### Проверка предзагрузки

1. Перезапустите сервер
2. Проверьте логи - должно быть сообщение о предзагрузке критичных модулей
3. Загрузите модуль и проверьте время ответа (должно быть быстрее)

## ✅ Чеклист тестирования

- [ ] Статус системы возвращает корректные данные
- [ ] Загрузка отдельного модуля работает
- [ ] Загрузка нескольких модулей работает
- [ ] Загрузка всех модулей работает
- [ ] Проверка версии работает
- [ ] Работает для русской локали
- [ ] Работает для английской локали
- [ ] Обработка ошибок для несуществующих модулей
- [ ] Checksum генерируется корректно
- [ ] Версия из package.json корректна
- [ ] Предзагрузка работает (проверить логи)

## 🐛 Отладка

### Проблема: 404 Not Found

**Причина:** Неправильный путь или сервер не запущен

**Решение:**
1. Проверьте, что сервер запущен на порту 3001
2. Проверьте правильность пути: `/api/v2/translations/...`

### Проблема: Module not found

**Причина:** Модуль не существует в списке доступных модулей

**Решение:**
1. Проверьте список доступных модулей в `AVAILABLE_MODULES`
2. Убедитесь, что файл модуля существует в `data/translations/v2/{locale}/`

### Проблема: Пустой ответ

**Причина:** Файл модуля пустой или не существует

**Решение:**
1. Проверьте наличие файла модуля
2. Проверьте содержимое файла (должен быть валидный JSON)

