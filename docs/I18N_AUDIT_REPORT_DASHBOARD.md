# Отчёт по аудиту i18n для страницы `/en/dashboard`

**Дата:** 2025-01-23  
**Страница:** `http://localhost:3000/en/dashboard`  
**Статус:** ❌ **FAIL**

---

## Резюме

Проведён аудит страницы `/en/dashboard` на корректность работы системы интернационализации. Обнаружены критические проблемы с переводами: ключи переводов не загружаются из API, множество русских строк в интерфейсе, проблемы с форматированием.

---

## Проверка базовой инфраструктуры

### ✅ Корректно работает:

- **URL:** `/en/dashboard` — корректный префикс языка
- **window.i18next.language:** `'en'` — язык установлен правильно
- **document.documentElement.lang:** `'en'` — атрибут HTML установлен
- **localStorage:** `{ state: { language: 'en' } }` — язык сохранён
- **API запросы:** Успешные запросы к `/api/v2/translations/en/profile` и `/api/v2/translations/en/dashboard`
- **Sidebar:** Все пункты меню на английском (`Profile`, `Data`, `Security`, `Family`, `Work`, `Payments`, `Support`)

---

## Обнаруженные проблемы

### 🔴 КРИТИЧЕСКИЕ ПРОБЛЕМЫ

#### 1. Ключи переводов не загружаются из API

**Проблема:** API `/api/v2/translations/en/profile` не возвращает секцию `personalData.documents` и `personalData.addresses`, хотя переводы есть в файле `backend-mock/data/translations/v2/en/profile.json`.

**Проявление:**
- В DOM отображаются сырые ключи: `personalData.documents.title`, `personalData.documents.viewAll`, `personalData.documents.description`
- Аналогично для адресов: `personalData.addresses.title`, `personalData.addresses.viewAll`, `personalData.addresses.addAddress`

**Компоненты:**
- `DocumentsGrid.tsx` (строки 84-85, 91)
- `AddressesGrid.tsx` (строки 72-73, 79, 146)

**Проверка API:**
```javascript
// Результат проверки через браузер:
{
  hasPersonalData: true,
  hasDocuments: false,  // ❌ Должно быть true
  documentsKeys: [],
  sample: "NOT FOUND"
}
```

**Причина:** Структура данных в ответе API не совпадает с ожидаемой. Возможно, проблема в обработке модуля `profile` на бэкенде.

---

### 🟡 СРЕДНИЕ ПРОБЛЕМЫ

#### 2. Русские строки в Header

**Компонент:** `Header.tsx`

**Проблемы:**
- **Строка 90:** `placeholder={t('common.search', 'Поиск...')}` — fallback на русском, но ключ `common.search` должен быть "Search..."
- **Строка 110:** `aria-label={t('header.notifications', 'Уведомления')}` — fallback на русском
- **Строка 142:** `aria-label={t('profile.menu', 'Меню профиля')}` — fallback на русском
- **Строка 231 (PageTemplate):** `title="Текущая тема: light. Кликните для переключения"` — хардкодная русская строка

**Ожидаемое поведение:**
- Placeholder поиска должен быть "Search..."
- aria-label для уведомлений: "Notifications"
- aria-label для меню профиля: "Profile menu"
- Tooltip темы должен быть переведён

---

#### 3. Русские строки в ProfileCard

**Компонент:** `ProfileCard.tsx`

**Проблемы:**
- **Строка 69:** `aria-label={t('profile.edit', 'Редактировать')}` — fallback на русском
- **Строка 110:** `{t('dashboard.mergeAccounts', 'Объединить аккаунты')}` — fallback на русском

**Ожидаемое поведение:**
- aria-label: "Edit"
- Текст ссылки: "Merge Accounts"

**Дополнительно:** Компонент `Avatar` показывает "Статус: онлайн" вместо "Status: online" — проблема в компоненте `Avatar`.

---

#### 4. Русские строки в виджетах Dashboard

**Компоненты:** `CoursesWidget.tsx`, `EventsWidget.tsx`, `RoadmapWidget.tsx`

**Проблемы:**
- **Кнопки управления виджетами:** "Перетащить виджет", "Удалить виджет" — хардкодные русские строки
- **Названия курсов:** "Основы программирования", "Дизайн интерфейсов", "Английский язык" — данные из API, но должны быть переведены
- **События:** "Оплачен курс 'Основы программирования'", "Ребенок получил достижение 'Первые шаги'" — русские строки в данных

**Ожидаемое поведение:**
- Кнопки: "Drag widget", "Remove widget"
- Данные должны приходить с переводами или использовать ключи переводов

---

#### 5. Русские строки в AddressesGrid

**Компонент:** `AddressesGrid.tsx`

**Проблемы:**
- **Типы адресов:** "Дом", "Работа", "Другие" — отображаются на русском вместо "Home", "Work", "Other"
- Используется функция `getAddressLabel()`, но она не переводит типы адресов

**Ожидаемое поведение:**
- Типы адресов должны использовать переводы из `personalData.addresses.home`, `personalData.addresses.work`, `personalData.addresses.other`

---

#### 6. Русские строки в DocumentsGrid

**Компонент:** `DocumentsGrid.tsx`

**Проблемы:**
- **Типы документов:** "Паспорт", "Загранпаспорт", "ВУ", "ОМС", "СНИЛС" — отображаются на русском
- Используется функция `getDocumentLabel()`, но она не переводит типы документов

**Ожидаемое поведение:**
- Типы документов должны использовать переводы из `personalData.documents.*`

---

#### 7. Русские строки в FamilyMembers

**Компонент:** `FamilyMembers.tsx`

**Проблемы:**
- **Статус:** "Статус: офлайн" — хардкодная русская строка вместо "Status: offline"

**Ожидаемое поведение:**
- Статус должен использовать переводы

---

#### 8. Проблемы с форматированием в SubscriptionsList

**Компонент:** `SubscriptionsList.tsx`

**Проблемы:**
- **Единицы измерения:** "мес" вместо "mo" или "month"
- **Форматирование валюты:** "От 150 000₽" вместо "From 150,000 ₽" (на английском должны быть запятые)
- **Текст:** "Бесплатно" вместо "Free"

**Ожидаемое поведение:**
- Единицы измерения должны переводиться
- Форматирование валюты должно соответствовать локали (запятые для EN, пробелы для RU)
- Текст должен использовать переводы

---

## Отладка

### Значения в консоли браузера:

```javascript
window.i18next.language; // 'en' ✅
document.documentElement.lang; // 'en' ✅
JSON.parse(localStorage.getItem('loginus-language')); 
// { state: { language: 'en' }, version: 0 } ✅
```

### Network запросы:

- ✅ `GET /api/v2/translations/en?modules=common,profile,dashboard` — 200 OK
- ✅ `GET /api/v2/translations/en/dashboard` — 200 OK
- ✅ `GET /api/v2/translations/en/profile` — 200 OK (но структура данных неполная)

### Логи консоли:

```
[i18n] [critical] add bundle for en: 10 keys [common, header, footer, sidebar, security, personalData, family, payment, work, menu]
[i18n-v2] Loaded en/dashboard from API
[i18n-v2] Loaded en/profile from API
```

**Проблема:** Модуль `profile` загружается, но секция `personalData.documents` отсутствует в ответе API.

---

## Примеры корректной работы

### ✅ Что работает правильно:

1. **Sidebar:** Все пункты меню корректно переводятся на английский
2. **Заголовок страницы:** "Profile" отображается на английском
3. **Виджеты:** Заголовки "My Learning Courses", "Events", "My Roadmap" на английском
4. **Кнопки:** "View All", "Full Version", "Add Widget" на английском
5. **Семейная группа:** Заголовок "Family" и кнопка "Manage" на английском
6. **Подписки:** Заголовок "Subscriptions" и кнопка "Details" на английском
7. **Форматирование валюты:** "RUB 12,500" — корректное форматирование с запятой для английского

---

## Рекомендации по исправлению

### Приоритет 1 (Критично):

1. **Исправить API для модуля `profile`:**
   - Проверить, почему секция `personalData.documents` не возвращается в ответе API
   - Убедиться, что структура данных в ответе соответствует файлу `backend-mock/data/translations/v2/en/profile.json`

2. **Исправить компоненты DocumentsGrid и AddressesGrid:**
   - Убедиться, что переводы загружаются из API
   - Добавить fallback на статические файлы, если API не возвращает данные

### Приоритет 2 (Важно):

3. **Исправить fallback значения в компонентах:**
   - Заменить все русские fallback на английские в `Header.tsx`, `ProfileCard.tsx`
   - Использовать английские fallback по умолчанию

4. **Исправить хардкодные строки:**
   - Перевести tooltip темы в `PageTemplate.tsx`
   - Перевести кнопки управления виджетами
   - Перевести статусы в `Avatar` и `FamilyMembers`

5. **Исправить функции перевода:**
   - Проверить `getDocumentLabel()` и `getAddressLabel()` — они должны использовать переводы
   - Убедиться, что типы документов и адресов переводятся через i18n

### Приоритет 3 (Желательно):

6. **Исправить форматирование:**
   - Единицы измерения должны переводиться
   - Форматирование валюты должно соответствовать локали

7. **Перевести данные из API:**
   - Названия курсов, событий должны приходить с переводами или использовать ключи переводов

---

## Следующие шаги

1. Проверить и исправить API для модуля `profile`
2. Исправить компоненты с проблемами переводов
3. Добавить автотесты для проверки отсутствия русских строк на английской версии
4. Повторить аудит после исправлений

---

## Приложение: Список всех найденных проблем

| Компонент | Проблема | Приоритет | Статус |
|-----------|----------|-----------|--------|
| API `/api/v2/translations/en/profile` | Не возвращает `personalData.documents` | Критично | ❌ |
| DocumentsGrid | Ключи переводов не работают | Критично | ❌ |
| AddressesGrid | Ключи переводов не работают | Критично | ❌ |
| Header | Русские fallback значения | Важно | ❌ |
| Header | Хардкодный tooltip темы | Важно | ❌ |
| ProfileCard | Русские fallback значения | Важно | ❌ |
| Avatar | "Статус: онлайн" вместо "Status: online" | Важно | ❌ |
| Виджеты | "Перетащить виджет", "Удалить виджет" | Важно | ❌ |
| AddressesGrid | "Дом", "Работа", "Другие" на русском | Важно | ❌ |
| DocumentsGrid | Типы документов на русском | Важно | ❌ |
| FamilyMembers | "Статус: офлайн" на русском | Важно | ❌ |
| SubscriptionsList | "мес", "Бесплатно" на русском | Средне | ❌ |
| SubscriptionsList | Неправильное форматирование валюты | Средне | ❌ |

---

**Отчёт составлен:** 2025-01-23  
**Аудитор:** AI Assistant  
**Версия системы:** Текущая (dev)

