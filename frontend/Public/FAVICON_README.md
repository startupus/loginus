# Фавиконы Loginus ID

## Обзор

Система фавиконов для Loginus ID разработана с учетом современных стандартов и поддерживает автоматическое переключение между светлой и темной темами браузера.

## Файлы фавиконов

### 1. `favicon.svg` (64x64)
Основной фавикон в формате SVG с адаптивной цветовой схемой:
- **Светлая тема (light mode)**: темный фон (#111827), белый текст
- **Темная тема (dark mode)**: светлый фон (#f8fafc), темный текст
- **Элементы**:
  - Текст "iD" (28px, жирный)
  - Зеленая точка индикатор (#22c55e)
  - Скругленные углы (rx=12)

### 2. `favicon-32x32.svg`
Средний размер фавикона (32x32):
- Уменьшенный размер текста (14px)
- Меньшая зеленая точка (r=3)
- Скругленные углы (rx=6)
- Те же адаптивные цвета

### 3. `favicon-16x16.svg`
Минимальный размер фавикона (16x16):
- Упрощенный дизайн: буква "i" + зеленая точка
- Зеленая точка (r=2.5)
- Скругленные углы (rx=3)
- Те же адаптивные цвета

## Технические детали

### Адаптивная цветовая схема

Фавиконы используют CSS media queries внутри SVG для автоматического переключения цветов:

```css
@media (prefers-color-scheme: light) {
  .bg { fill: #111827; }    /* Темный фон */
  .text { fill: #ffffff; }   /* Белый текст */
}

@media (prefers-color-scheme: dark) {
  .bg { fill: #f8fafc; }     /* Светлый фон */
  .text { fill: #0f172a; }   /* Темный текст */
}
```

### HTML подключение

В файле `index.html`:

```html
<!-- Фавиконы с адаптивной поддержкой светлой и темной темы -->
<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
<link rel="icon" type="image/svg+xml" sizes="32x32" href="/favicon-32x32.svg" />
<link rel="icon" type="image/svg+xml" sizes="16x16" href="/favicon-16x16.svg" />
<link rel="apple-touch-icon" href="/favicon.svg" />

<!-- Цвет темы для светлой и темной темы -->
<meta name="theme-color" content="#ffffff" media="(prefers-color-scheme: light)" />
<meta name="theme-color" content="#0f172a" media="(prefers-color-scheme: dark)" />
```

## Поддержка браузеров

### Полная поддержка (SVG + media queries):
- ✅ Chrome 89+
- ✅ Firefox 96+
- ✅ Safari 15+
- ✅ Edge 89+

### Частичная поддержка (только SVG, без автоматического переключения темы):
- ⚠️ Chrome 76-88
- ⚠️ Firefox 41-95
- ⚠️ Safari 12-14

### Apple устройства:
- ✅ iOS Safari 15+ (с адаптивными цветами)
- ✅ macOS Safari 15+ (с адаптивными цветами)
- ⚠️ Старые версии (базовый SVG без переключения темы)

## Сборка и деплой

При сборке проекта фавиконы автоматически копируются из `frontend/public/` в `frontend/dist/`:

```bash
# При сборке Vite автоматически копирует файлы из public
npm run build

# Или вручную:
cp frontend/public/favicon*.svg frontend/dist/
```

## Цветовая палитра

| Элемент | Светлая тема | Темная тема |
|---------|--------------|-------------|
| Фон | #111827 | #f8fafc |
| Текст | #ffffff | #0f172a |
| Зеленая точка | #22c55e | #22c55e |

## Обслуживание

### Изменение цветов

Чтобы изменить цвета фавиконов:

1. Откройте нужный файл (`favicon.svg`, `favicon-32x32.svg`, `favicon-16x16.svg`)
2. Найдите секцию `<style>` с media queries
3. Измените значения `fill` в соответствующих правилах
4. Пересоберите проект

### Добавление дополнительных размеров

1. Создайте новый SVG файл с нужным размером
2. Скопируйте структуру из существующих файлов
3. Адаптируйте размеры элементов (текст, круги, border-radius)
4. Добавьте link в `index.html`

## Best Practices

1. **Всегда используйте SVG формат** для масштабируемости и поддержки адаптивных цветов
2. **Тестируйте в обеих темах** - проверяйте отображение в светлой и темной теме браузера
3. **Учитывайте контраст** - убедитесь, что фавикон читаем на любом фоне
4. **Оптимизируйте размер** - минимизируйте SVG код, но сохраняйте читаемость
5. **Синхронизируйте с брендингом** - цвета фавикона должны соответствовать дизайн-системе

## Соответствие дизайн-системе

Фавиконы используют цвета из дизайн-системы Loginus UI:

- `#111827` - `dark-1` (фон в светлой теме)
- `#f8fafc` - `slate-50` (фон в темной теме)  
- `#0f172a` - `slate-900` (текст в темной теме)
- `#22c55e` - `success` (зеленая точка индикатор)

## Тестирование

### Ручное тестирование:

1. Откройте приложение в браузере
2. Проверьте отображение фавикона во вкладке
3. Переключите тему ОС (macOS: System Preferences → General → Appearance)
4. Убедитесь, что фавикон автоматически изменился
5. Проверьте на мобильных устройствах (iOS Safari)

### Автоматизированное тестирование:

```bash
# Используйте Playwright для тестирования
npm run test:e2e
```

## История изменений

### v1.1 (2024-11-22)
- ✨ Добавлена адаптивная поддержка светлой и темной темы
- ✨ Реализованы три размера фавиконов (64x64, 32x32, 16x16)
- ✨ Добавлены адаптивные meta теги theme-color
- 📝 Создана документация

### v1.0 (ранее)
- ✨ Первоначальная версия с фиксированным темным фоном

