# Руководство по использованию темы в Loginus UI

## 📋 Содержание

1. [Введение](#введение)
2. [Основные принципы](#основные-принципы)
3. [Классы темы](#классы-темы)
4. [Примеры использования](#примеры-использования)
5. [Миграция со старых классов](#миграция-со-старых-классов)
6. [Лучшие практики](#лучшие-практики)

## Введение

Дизайн-система Loginus UI использует централизованную систему тем через CSS переменные и Tailwind CSS. Все компоненты должны использовать единые классы темы для обеспечения консистентности и поддержки темной/светлой темы.

## Основные принципы

### 1. Single Source of Truth
Все токены темы определены в `frontend/src/design-system/themes/` и доступны через CSS переменные.

### 2. Theme-driven Design
Компоненты используют CSS переменные через Tailwind классы, что позволяет динамически менять тему без перезагрузки страницы.

### 3. Dark Mode Support
Темная тема активируется через класс `dark` на элементе `html`. Tailwind автоматически применяет классы с префиксом `dark:`.

## Классы темы

### Фоновые цвета

#### Основной фон
```tsx
// ✅ Правильно - использует CSS переменные
<div className="bg-background dark:bg-surface">
  Контент
</div>

// ❌ Неправильно - хардкод цветов
<div className="bg-white dark:bg-gray-800">
  Контент
</div>
```

#### Карточки и секции
```tsx
// ✅ Правильно
<div className="bg-white dark:bg-surface">
  Карточка
</div>

// Для ProfileCard и других важных карточек используем белый фон в светлой теме
<div className="bg-white dark:bg-surface">
  ProfileCard
</div>
```

#### Вторичные фоны
```tsx
// ✅ Правильно
<div className="bg-gray-1 dark:bg-gray-2">
  Вторичный контент
</div>
```

### Цвета текста

#### Основной текст
```tsx
// ✅ Правильно
<p className="text-text-primary">
  Основной текст
</p>

// ❌ Неправильно
<p className="text-dark dark:text-white">
  Основной текст
</p>
```

#### Вторичный текст
```tsx
// ✅ Правильно
<p className="text-text-secondary">
  Вторичный текст
</p>

// ❌ Неправильно
<p className="text-body-color dark:text-dark-6">
  Вторичный текст
</p>
```

#### Отключенный текст
```tsx
// ✅ Правильно
<p className="text-text-disabled">
  Отключенный текст
</p>
```

### Границы

```tsx
// ✅ Правильно
<div className="border border-border">
  Контент с границей
</div>

// ❌ Неправильно
<div className="border-stroke dark:border-dark-3">
  Контент с границей
</div>
```

### Семантические цвета

#### Primary (основной цвет бренда)
```tsx
<button className="bg-primary text-white">
  Кнопка
</button>

<span className="text-primary">
  Акцентный текст
</span>
```

#### Success (успех)
```tsx
<div className="bg-success/10 text-success">
  Успешное сообщение
</div>
```

#### Error (ошибка)
```tsx
<div className="bg-error/10 text-error">
  Ошибка
</div>
```

#### Warning (предупреждение)
```tsx
<div className="bg-warning/10 text-warning">
  Предупреждение
</div>
```

#### Info (информация)
```tsx
<div className="bg-info/10 text-info">
  Информация
</div>
```

## Примеры использования

### Простая карточка
```tsx
export const Card: React.FC<{ children: React.ReactNode }> = ({ children }) => {
  return (
    <div className="bg-background dark:bg-surface rounded-xl p-6 border border-border">
      {children}
    </div>
  );
};
```

### Карточка с заголовком и текстом
```tsx
export const InfoCard: React.FC<{ title: string; description: string }> = ({ title, description }) => {
  return (
    <div className="bg-white dark:bg-surface rounded-xl p-6 border border-border">
      <h3 className="text-lg font-semibold text-text-primary mb-2">
        {title}
      </h3>
      <p className="text-sm text-text-secondary">
        {description}
      </p>
    </div>
  );
};
```

### Кнопка с поддержкой темы
```tsx
export const ThemedButton: React.FC<{ children: React.ReactNode }> = ({ children }) => {
  return (
    <button className="bg-primary hover:bg-primary/90 text-white px-4 py-2 rounded-lg transition-colors">
      {children}
    </button>
  );
};
```

### Форма с полями ввода
```tsx
export const Form: React.FC = () => {
  return (
    <form className="space-y-4">
      <div>
        <label className="block text-sm font-medium text-text-primary mb-2">
          Email
        </label>
        <input
          type="email"
          className="w-full px-4 py-2 bg-background dark:bg-surface border border-border rounded-lg text-text-primary focus:outline-none focus:ring-2 focus:ring-primary"
        />
      </div>
    </form>
  );
};
```

## Миграция со старых классов

### Таблица замен

| Старый класс | Новый класс | Описание |
|-------------|-------------|----------|
| `bg-white dark:bg-dark-2` | `bg-background dark:bg-surface` | Основной фон |
| `bg-white dark:bg-dark-2` | `bg-white dark:bg-surface` | Фон карточек (для ProfileCard и важных элементов) |
| `text-dark dark:text-white` | `text-text-primary` | Основной текст |
| `text-body-color dark:text-dark-6` | `text-text-secondary` | Вторичный текст |
| `border-stroke dark:border-dark-3` | `border-border` | Границы |
| `bg-gray-1 dark:bg-dark-3` | `bg-gray-1 dark:bg-gray-2` | Вторичные фоны |
| `bg-error/10 dark:bg-error/20` | `bg-error/10` | Фон ошибок (прозрачность работает автоматически) |

### Автоматическая замена

Для массовой замены используйте поиск и замену в вашем редакторе:

1. `bg-white dark:bg-dark-2` → `bg-background dark:bg-surface` (или `bg-white dark:bg-surface` для карточек)
2. `text-dark dark:text-white` → `text-text-primary`
3. `text-body-color dark:text-dark-6` → `text-text-secondary`
4. `border-stroke dark:border-dark-3` → `border-border`
5. `bg-gray-1 dark:bg-dark-3` → `bg-gray-1 dark:bg-gray-2`
6. `bg-error/10 dark:bg-error/20` → `bg-error/10`

## Лучшие практики

### ✅ Делайте правильно

1. **Используйте семантические классы**
   ```tsx
   // ✅ Хорошо
   <div className="bg-background dark:bg-surface">
   
   // ❌ Плохо
   <div className="bg-white dark:bg-gray-800">
   ```

2. **Используйте CSS переменные через Tailwind**
   ```tsx
   // ✅ Хорошо - автоматически адаптируется к теме
   <div className="text-text-primary">
   
   // ❌ Плохо - хардкод цветов
   <div className="text-gray-900 dark:text-white">
   ```

3. **Группируйте связанные классы**
   ```tsx
   // ✅ Хорошо - читаемо
   <div className="bg-background dark:bg-surface border border-border rounded-xl p-6">
   
   // ❌ Плохо - разбросано
   <div className="bg-background rounded-xl border dark:bg-surface p-6 border-border">
   ```

4. **Используйте прозрачность для семантических цветов**
   ```tsx
   // ✅ Хорошо - автоматически адаптируется
   <div className="bg-error/10 text-error">
   
   // ❌ Плохо - дублирование для темной темы
   <div className="bg-error/10 dark:bg-error/20 text-error">
   ```

### ❌ Избегайте

1. **Хардкод цветов**
   ```tsx
   // ❌ Плохо
   <div className="bg-white dark:bg-gray-800 text-gray-900 dark:text-white">
   ```

2. **Дублирование классов для темной темы, когда это не нужно**
   ```tsx
   // ❌ Плохо - прозрачность работает автоматически
   <div className="bg-primary/10 dark:bg-primary/20">
   
   // ✅ Хорошо
   <div className="bg-primary/10">
   ```

3. **Использование устаревших классов**
   ```tsx
   // ❌ Плохо - устаревшие классы
   <div className="text-dark dark:text-white">
   ```

## Проверка компонентов

После создания или изменения компонента убедитесь, что:

1. ✅ Используются правильные классы темы
2. ✅ Компонент корректно отображается в светлой теме
3. ✅ Компонент корректно отображается в темной теме
4. ✅ Нет хардкода цветов
5. ✅ Нет устаревших классов

## Полезные ссылки

- [Tailwind CSS Dark Mode](https://tailwindcss.com/docs/dark-mode)
- [Дизайн-система Loginus UI](./src/design-system/README.md)
- [Токены темы](./src/design-system/themes/)

## Вопросы и поддержка

Если у вас возникли вопросы по использованию темы, обратитесь к команде разработки или создайте issue в репозитории проекта.

