# Банк компонентов TailGrids

**Назначение:** Полная коллекция платных компонентов TailGrids Pro из корпоративного репозитория

**Источник:** https://github.com/startupus/ui-ux-components/tree/main/react-pro-components-main

---

## Структура

### 📦 core/ - CoreComponents (базовые компоненты)

**Primitives (атомарные):**
- `Buttons/` - 34 варианта кнопок (Primary, Secondary, Outline, Rounded, With Icons)
- `FormElement/` - 5 вариантов полей ввода
- `Badges/` - 9 вариантов значков
- `Avatar/` - 9 вариантов аватаров
- `Alerts/` - 13 вариантов уведомлений
- `Switch/` - переключатели
- `Tab/` - вкладки
- `Tooltip/` - подсказки
- `Toast/` - всплывающие уведомления
- `Progress/` - индикаторы прогресса
- `Spinner/` - загрузчики
- И многие другие...

### 📦 application/ - ApplicationComponents (компоненты приложений)

**Molecules (молекулярные):**
- `Card/` - 16 вариантов карточек
- `Modal/` - 11 вариантов модальных окон
- `Table/` - 12 вариантов таблиц
- `Navbar/` - 8 вариантов навигации
- `Footer/` - 7 вариантов футеров
- `Signin/` - 8 вариантов форм входа
- `Contact/` - 14 вариантов контактных форм
- `Blog/` - 10 вариантов блогов
- `Error/` - 8 вариантов страниц ошибок
- И многие другие...

### 📦 dashboard/ - DashboardComponents (компоненты дашборда)

**Organisms (организмы):**
- `DataStats/` - 10 вариантов статистики
- `Chart/` - 10 вариантов графиков
- `Calendar/` - календари
- `Profile/` - 5 вариантов профилей
- `SettingsPage/` - 10 вариантов настроек
- `ChatBox/` - чаты
- `Dropdown/` - выпадающие меню
- И многие другие...

---

## Использование банка

### Процесс адаптации компонента:

**Шаг 1: Найти нужный компонент**
```bash
# Например, ищем кнопку
ls design-system/tailgrids-bank/core/Buttons/
```

**Шаг 2: Изучить исходник**
```bash
# Открыть и изучить
cat design-system/tailgrids-bank/core/Buttons/PrimaryButton.jsx
```

**Шаг 3: Скопировать в рабочую директорию**
```bash
# Если нужен новый компонент
cp design-system/tailgrids-bank/core/Buttons/PrimaryButton.jsx \
   design-system/primitives/Button/Button-temp.jsx
```

**Шаг 4: Конвертировать JSX → TSX**
- Добавить `import React from 'react'`
- Создать TypeScript интерфейс Props
- Использовать `React.FC<Props>`
- Переименовать в `.tsx`

**Шаг 5: Добавить в дизайн-систему**
- Сохранить в правильной директории
- Экспортировать через index.ts
- Использовать на страницах

---

## Примеры компонентов

### Button из банка:

**Файл:** `core/Buttons/PrimaryButton.jsx`
```jsx
const PrimaryButton = () => {
  return (
    <button className="inline-flex items-center justify-center border border-primary bg-primary px-7 py-3 text-center text-base font-medium text-white hover:border-[#1B44C8] hover:bg-[#1B44C8] active:border-[#1B44C8] active:bg-[#1B44C8] disabled:border-gray-3 disabled:bg-gray-3 disabled:text-dark-5">
      Button
    </button>
  );
};
```

### Card из банка:

**Файл:** `application/Card/Card1.jsx`
```jsx
const SingleCard = ({ image, Button, CardDescription, CardTitle }) => {
  return (
    <div className="mb-10 overflow-hidden rounded-lg bg-white shadow-1 duration-300 hover:shadow-3 dark:bg-dark-2 dark:shadow-card dark:hover:shadow-3">
      <img src={image} alt="" className="w-full" />
      <div className="p-8 text-center sm:p-9 md:p-7 xl:p-9">
        <h3>
          <a href="#" className="mb-4 block text-xl font-semibold text-dark hover:text-primary dark:text-white">
            {CardTitle}
          </a>
        </h3>
        <p className="mb-7 text-base leading-relaxed text-body-color dark:text-dark-6">
          {CardDescription}
        </p>
      </div>
    </div>
  );
};
```

### Modal из банка:

**Файл:** `application/Modal/Modal1.jsx`
```jsx
const Modal = () => {
  const [modalOpen, setModalOpen] = useState(false);
  
  return (
    <div className={`fixed left-0 top-0 flex h-full min-h-screen w-full items-center justify-center bg-dark/90 px-4 py-5 ${modalOpen ? "block" : "hidden"}`}>
      <div className="w-full max-w-[570px] rounded-[20px] bg-white px-8 py-12 text-center dark:bg-dark-2 md:px-[70px] md:py-[60px]">
        {/* Content */}
      </div>
    </div>
  );
};
```

---

## Ключевые классы TailGrids

### Цвета:
- `bg-primary` - основной цвет
- `bg-secondary` - вторичный цвет
- `text-dark` - темный текст → `dark:text-white`
- `text-body-color` - основной текст → `dark:text-dark-6`
- `border-stroke` - границы → `dark:border-dark-3`

### Фоны:
- `bg-white` → `dark:bg-dark-2` (карточки)
- `bg-gray-1` → `dark:bg-dark` (страницы)
- `bg-gray-2` → `dark:bg-dark-3` (hover)

### Тени:
- `shadow-1` - легкая тень
- `shadow-3` - средняя тень
- `dark:shadow-card` - тень для карточек в темной теме

---

## Статистика банка

**Всего компонентов:** 200+ файлов .jsx

**По категориям:**
- Core: ~100 компонентов
- Application: ~80 компонентов  
- Dashboard: ~30 компонентов

**Готовы к использованию:** Все компоненты можно использовать сразу (JSX) или адаптировать (TSX)

---

## Правила работы с банком

### ✅ Можно:
- Копировать компоненты для адаптации
- Изучать структуру и классы
- Использовать как референс
- Брать несколько вариантов и комбинировать

### ❌ Нельзя:
- Менять файлы в банке (только read-only)
- Удалять компоненты из банка
- Импортировать напрямую из банка на страницах

### 📝 Процесс:
1. Найти в банке
2. Скопировать в рабочую директорию
3. Адаптировать (JSX→TSX)
4. Использовать в продакшене

---

**Создано:** 17 ноября 2025  
**Назначение:** Справочник и источник для адаптации компонентов

